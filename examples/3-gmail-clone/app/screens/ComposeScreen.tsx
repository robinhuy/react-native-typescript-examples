import React, {FunctionComponent} from 'react';
import {StyleSheet, Text} from 'react-native';

const ComposeScreen: FunctionComponent = () => {
  return (
    <>
      <Text>Compose</Text>
    </>
  );
};

const styles = StyleSheet.create({});

export default ComposeScreen;
